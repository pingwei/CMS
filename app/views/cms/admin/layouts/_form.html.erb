<p class="form">基本情報</p>

<table class="show">
  <%= concept_form f %>
  <tr>
    <th><%=h @item.locale(:name) %></th>
    <td><%= f.text_field :name, :class => 'id' %></td>
  </tr><tr>
    <th><%=h @item.locale(:title) %></th>
    <td><%= f.text_field :title, :class => 'name' %></td>
  </tr>
</table>

<br />
<table style="width: 100%"><tr><td style="width: 49%; vertical-align: top;">
  <p class="form">標準レイアウト</p>
  <table class="show">
    <tr>
      <th><%=h @item.locale(:head) %></th></tr><tr>
      <td><%= f.text_area :head, :class => 'head' %></td>
    </tr><tr>
      <th><%=h @item.locale(:body) %></th></tr><tr>
      <td><%= f.text_area :body, :class => 'body' %></td>
    </tr><tr>
      <th><%=h @item.locale(:stylesheet) %></th></tr><tr>
      <td><%= f.text_area :stylesheet, :class => 'body' %></td>
    </tr>
  </table>
</td><td style="width: 15px;"></td><td style="vertical-align: top;">
  <p class="form">携帯レイアウト</p>
  <table class="show">
    <tr>
      <th><%=h @item.locale(:mobile_head) %></th></tr><tr>
      <td><%= f.text_area :mobile_head, :class => 'head' %></td>
    </tr><tr>
      <th><%=h @item.locale(:mobile_body) %></th></tr><tr>
      <td><%= f.text_area :mobile_body, :class => 'body' %></td>
    </tr><tr>
      <th><%=h @item.locale(:mobile_stylesheet) %></th></tr><tr>
      <td><%= f.text_area :mobile_stylesheet, :class => 'body' %></td>
    </tr>
  </table>
</td></tr></table>

<%= creator_form f %>
