<table class="index">
  
  <tr>
    <th style="width: 60px; text-align: center;">表示</th>
    <th style="width: 60px; text-align: right;">ID</th>
    <th style="width: 20%;">サイト名称</th>
    <th style="width: ;">サイトURL</th>
    <th style="width: ;">携帯URL</th>
    <th style="width: 60px; text-align: center;">状態</th>
  </tr>
  
  <% for item in @items %>
  <tr <%= cycle '', 'class="cycle"' %>>
    <td style="text-align: center;"><%= link_to :show, url_for(:action => :show, :id => item) %></td>
    <td style="text-align: right;"><%=h item.id %></td>
    <td><%=h item.name %></td>
    <td><%=h item.full_uri %></td>
    <td><%=h item.mobile_full_uri %></td>
    <td style="text-align: center;"><%= item.status.name %></td>
  </tr>
  <% end %>

</table>

<%= paginate @items %>
