<p class="form">基本情報</p>

<table class="show">
  <tr>
    <th><%= @item.locale(:parent_id) %></th>
    <td><%=h safe{@item.parent.name} %></td>
  </tr><tr>
    <th><%= @item.locale(:name) %></th>
    <td><%=h @item.name %></td>
  </tr><tr>
    <th><%= @item.locale(:state) %></th>
    <td><%=h @item.status.name %></td>
  </tr><tr>
    <th><%= @item.locale(:sort_no) %></th>
    <td><%=h @item.sort_no %></td>
  </tr>
</table>

<%= creator_view @item %>
