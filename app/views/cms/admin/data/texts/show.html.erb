<table class="show">
  <tr>
    <th><%=h @item.locale(:concept_id) %></th>
    <td><%=h safe{@item.concept.name} %> </td>
  </tr><tr>
    <th><%=h @item.locale(:state) %></th>
    <td><%=h safe{@item.status.name} %> </td>
  </tr><tr>
    <th><%=h @item.locale(:name) %></th>
    <td><%=h @item.name %></td>
  </tr><tr>
    <th><%=h @item.locale(:title) %></th>
    <td><%=h @item.title %></td>
  </tr>
</table>

<table class="show" style="margin: 0px;">
  <tr>
    <th><%= @item.locale(:body) %></th></tr><tr>
    <td><%=hbr @item.body %></td>
  </tr>
</table>

<%= creator_view @item %>
